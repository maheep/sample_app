<h1>Listing blogs</h1>
		 <%= image_tag current_user.photo.file? ? current_user.photo.url(:avatar) : "http://placehold.it/955x955" %> </td>
			<table>
		    <!--<th>Title</th>-->
		    <!--<th>Discription</th> -->
		    <th></th>
		    <th></th>
		    <th></th>
		  </tr>
		<% @blogs.each do |blog| %>
		  <tr>
		    <td><%= blog.title %></td>
		  </tr>
		  <tr>
		    <td><%= blog.discription %></td>
		  </tr>
		  <tr>
		    <td><%= link_to("Like it", blog_add_vote_path(blog, type: "like"), method: "get") %></td>
    <td><%= link_to("dislike it", blog_add_vote_path(blog, type: "unlike"), method: "get") %></td>
	 <td>Like Count(<%=blog.reputation_for(:votes).to_i%>)</td>
		  </tr>
		<% end %>
		</table>
	<table>
		<%= will_paginate @blogs %>
    <% @recent_blogs.each do |recent_blog| %>
		<tr>	
    		<td><%= link_to recent_blog.title, recent_blog %></td>
		</tr>

	<% end %>
	</table>
<%= link_to 'New Blog', new_blog_path %>
